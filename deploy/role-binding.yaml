apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: config-sync-mcp
  labels:
    app: config-sync-mcp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: config-sync-mcp
subjects:
  - kind: ServiceAccount
    name: config-sync-mcp
