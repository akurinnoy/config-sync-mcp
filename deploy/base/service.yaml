apiVersion: v1
kind: Service
metadata:
  name: config-sync-mcp
  labels:
    app: config-sync-mcp
spec:
  selector:
    app: config-sync-mcp
  ports:
    - port: 8089
      targetPort: 8089
      name: mcp
