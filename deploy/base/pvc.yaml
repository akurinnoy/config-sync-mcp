apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-sync-storage
  labels:
    app: config-sync-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
