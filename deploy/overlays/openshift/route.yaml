apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: config-sync-mcp
  labels:
    app: config-sync-mcp
spec:
  to:
    kind: Service
    name: config-sync-mcp
    weight: 100
  port:
    targetPort: 8089
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
