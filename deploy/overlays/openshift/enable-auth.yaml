apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-sync-mcp
spec:
  template:
    spec:
      containers:
        - name: config-sync-mcp
          env:
            - name: CONFIG_SYNC_AUTH_ENABLED
              value: "true"
